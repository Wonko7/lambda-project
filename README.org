* all
#+begin_src shell
sudo cp -r guix/system/ /etc/guix-os
sudo -i HOST=`hostname` `which guix` system reconfigure /etc/guix-os/system/config.scm
guix home reconfigure guix/home/wonko.scm
#+end_src
