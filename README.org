* introduction
I have achieved peak computing.
* usage
#+begin_src shell
  cd guix
  # testing
  sudo $(SHIP=`hostname` guix system vm os.scm) -nic user,model=virtio-net-pci,hostfwd=tcp::10022-:22
  guix home container wonko.scm
  # deploy
  sudo SHIP=`hostname` `which guix` system reconfigure os.scm
  SHIP=`hostname` guix home reconfigure wonko.scm
#+end_src
